rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    // Admin Users Collection - Authenticated users can read
    match /admin-users/{document=**} {
      allow read: if isAuthenticated();
      allow write: if false;
    }

    // Applications - Public read and write (no authentication required for admissions)
    match /applications/{document=**} {
      allow read: if true;
      allow write: if true;
    }

    // OBI Applications - Public read and write (no authentication required for admissions)
    match /obi-applications/{document=**} {
      allow read: if true;
      allow write: if true;
    }

    // Admissions - Public read and write (no authentication required for admissions)
    match /admissions/{document=**} {
      allow read: if true;
      allow write: if true;
    }

    // Students - Authenticated users can read and write
    match /students/{document=**} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }

    // Site Settings - Public read, Authenticated write
    match /site-settings/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Contacts - Public write, Authenticated read
    match /contacts/{document=**} {
      allow read: if isAuthenticated();
      allow write: if true;
    }

    // News - Public read, Authenticated write
    match /news/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Events - Public read, Authenticated write
    match /events/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Courses - Public read, Authenticated write
    match /courses/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Gallery - Public read, Authenticated write
    match /gallery/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Leadership - Public read, Authenticated write
    match /leadership/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Ministries - Public read, Authenticated write
    match /ministries/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Statistics - Public read, Authenticated write
    match /statistics/{document=**} {
      allow read: if true;
      allow write: if isAuthenticated();
    }
  }
}

